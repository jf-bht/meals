# apps/mobile (Platzhalter)

Dieser Workspace ist noch nicht mit `create-expo-app` initialisiert. Er existiert
im Turborepo-Grundgerüst, damit `npm install` und `turbo run` bereits jetzt über
alle Workspaces hinweg funktionieren.

Geplanter Stack (siehe [Constitution/techstack.md](../../Constitution/techstack.md)):
React Native + Expo, NativeWind, TypeScript strict.

Wird in einem späteren Schritt gescaffoldet, sobald Onboarding-Flow (REQ-001)
und Wochenplan-Anzeige (REQ-004) implementiert werden.
